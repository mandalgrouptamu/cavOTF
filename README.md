# cavotf

CavOTF provides tools for running cavity molecular dynamics workflows with DFTB resources and helper scripts. The package exposes a CLI (`cavotf`) and module entry point (`python -m cavotf`) for launching simulations and validating workflows.

